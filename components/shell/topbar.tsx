/* Thin sticky header bar shown above the main content area */
export function TopBar() {
  return (
    <header className="sticky top-0 z-10 bg-bg-1 border-b border-border flex items-center gap-3 px-6 h-14 flex-shrink-0" />
  )
}
